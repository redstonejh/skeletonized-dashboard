# Conductor

Selected roles: conductor, planner, worker, critic, acceptance_gate.

The task is a targeted refactor/debugging run. The floor calculation is load-bearing for both direct drag placement and collision displacement, so the smallest useful team is the core roster.

Delegation used existing distinct sub-agent contexts because new spawn attempts were blocked by the active thread cap.
