# Planner Notes

Planned an inventory-first pass over `widget-registry.js`: remove all `widgetHint` and config prompt render paths, cover all 11 registered widget definitions, and preserve ECharts, Leaflet, TanStack, Monaco, wells, and `data.rows`.

