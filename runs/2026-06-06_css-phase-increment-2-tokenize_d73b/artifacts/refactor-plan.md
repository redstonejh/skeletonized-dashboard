# Refactor Plan

Tokenization-only CSS refactor. Add five primitive tokens to `tokens.css`, then replace repeated literal `gap` lengths and pill radii in `themes.css` and `dashboard-grid.css`. No selectors, cascade order, media queries, or `!important` flags are changed.
