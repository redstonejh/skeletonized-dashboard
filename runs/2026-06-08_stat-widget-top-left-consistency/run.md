# Stat Widget Top-Left Consistency

## Task
Make stat widgets display their number in the same top-left position on the dashboard and inside panels.

## Plan
- Use real delegated MAW role sessions and record proof.
- Inspect the stat widget render path and CSS layout rules.
- Change the shared stat shell alignment so panel-internal stat widgets inherit the same top-left placement as dashboard stat widgets.
- Commit and push without running tests, per explicit user instruction.

## Outcome
SHIP

Tests and validation were not run because the user explicitly instructed not to run tests or validation.
