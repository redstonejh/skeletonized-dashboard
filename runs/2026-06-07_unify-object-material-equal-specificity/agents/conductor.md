# Conductor Notes

Selected the core MAW roster for a frontend-ui-task. Real delegation was available through `multi_agent_v1.spawn_agent`; each role has a distinct agent id in `artifacts/delegation-proof.json`.

Critical invariants:

- Do not lower canonical selector specificity.
- Keep photo object material unchanged.
- Make static object material equal photo object material.
- Remove duplicate static computed-value replay blocks.
- No dependency or Electron changes.

