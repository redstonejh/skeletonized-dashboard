# Salvage Plan

Conservative deletion pass: classify mixed context/query symbols, keep active compatibility behavior, delete only graph/text-proven dormant origin registry and unused app-level bindings.
