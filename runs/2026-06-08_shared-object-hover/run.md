# Shared Object Hover

## Task
Remove the panel hover white top bar and apply one subtle shared hover treatment to panels and widgets.

## Delegation
Real MAW delegation used existing distinct sub-agent sessions because the runtime had already reached its sub-agent spawn limit.

## Outcome
SHIP. Added a final shared hover layer for dashboard widgets, panel-internal widgets, and panels that uses the existing surface-response class: a slight raise, faint diagonal shine, and no border/shadow stack. Panel header/body hover edge paint remains suppressed.

## Verification
Per user instruction, no tests, canaries, or validation commands were run.
