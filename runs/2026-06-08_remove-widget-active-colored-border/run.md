# Remove Widget Active Colored Border

## Task
Remove the remaining extra colored border around widgets so their edge matches panels.

## Plan
- Use real delegated MAW role sessions and record proof.
- Re-investigate widget visual edge sources rather than relying on the prior border-color patch.
- Neutralize the widget-only `.stat-card` hover/active blue border and outline.
- Commit and push without running tests, per explicit user instruction.

## Outcome
SHIP

Tests and validation were not run because the user explicitly instructed not to run tests or validation.
