# Planner Notes

Recommended using stable per-tab page state and attaching page activation after the existing widget/panel runtimes are available. The implementation kept the existing `builder` layout compatible for tab 1 while new pages persist through the page store.
