# Planner Notes

Plan: keep existing control nodes in the DOM, convert the nav to a fixed floating panel, add a gear trigger, persist drag position, mount one WebGL panel canvas, and update tests to open controls through the gear.
