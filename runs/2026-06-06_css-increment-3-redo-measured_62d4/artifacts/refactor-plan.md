# Refactor Plan

CSS increment 3 REDO stopped after Phase 1 measurement. The measurement shows the remaining per-tone CSS is largely non-duplicative: only 2 of 165 tone-specific declarations are still exact collapsible duplicates, with one estimated removable line. No further collapse was attempted because the decision gate allows a measured small-collapsible outcome.
